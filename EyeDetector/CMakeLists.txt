cmake_minimum_required(VERSION 2.8)
project(EyeDetector)
find_package(OpenCV REQUIRED)
include_directories(${OpenCV_INCLUDE_DIRS})
add_executable(EyeDetector eyedetect.cpp config.h)
target_link_libraries(EyeDetector ${OpenCV_LIBS})
